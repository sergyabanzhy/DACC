<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto" xmlns:tools="http://schemas.android.com/tools"
        android:orientation="vertical"
        android:background="?selectableItemBackground"
        android:layout_width="match_parent"
        android:layout_height="80dp">

    <ImageView android:layout_width="60dp"
               android:layout_height="60dp"
               app:layout_constraintTop_toTopOf="parent"
               app:layout_constraintBottom_toBottomOf="parent"
               android:src="@drawable/ic_local_taxi"
               app:layout_constraintStart_toStartOf="parent"
               tools:src="@tools:sample/avatars"
               android:layout_marginStart="8dp"
               android:id="@+id/ivCarIcon"/>

    <TextView android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:id="@+id/tvType"
              tools:text="@tools:sample/first_names"
              app:layout_constraintStart_toEndOf="@+id/ivCarIcon"
              android:layout_marginStart="8dp"
              app:layout_constraintTop_toTopOf="@+id/ivCarIcon"
              app:layout_constraintBottom_toBottomOf="@+id/ivCarIcon"/>

</androidx.constraintlayout.widget.ConstraintLayout>